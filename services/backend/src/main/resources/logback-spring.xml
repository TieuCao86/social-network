<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- =======================================================
         Console Appender
         ======================================================= -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Thêm màu sắc (highlight, cyan) và viết trên 1 dòng để tránh khoảng trắng thừa -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- =======================================================
         Application Loggers
         ======================================================= -->
    <!-- Log chi tiết cho code ứng dụng -->
    <logger name="com.socialnetwork" level="DEBUG" />

    <!-- Ẩn các log quá chi tiết của Spring / Hibernate nếu không cần thiết -->
    <logger name="org.springframework" level="INFO" />

    <!-- =======================================================
         Root Logger
         ======================================================= -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>